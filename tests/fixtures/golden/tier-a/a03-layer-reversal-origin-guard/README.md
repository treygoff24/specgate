# A03: layer-reversal-origin-guard

- **Tier:** A (P0)
- **Maps from Tier B:** C08
- **Target rule:** `enforce-layer`

Intro reverses layer direction (`domain` importing `ingress`).
Fix keeps dependency inside domain layer.
