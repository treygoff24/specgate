version: "2.3"
module: api
boundaries:
  path: src/api/**
  contracts:
    - id: api_contract
      contract: contracts/api.json
      match:
        files:
          - src/api/handler.ts
      envelope: required
constraints: []
