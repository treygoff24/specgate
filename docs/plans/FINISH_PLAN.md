# Specgate — Finish Everything Plan

**Goal:** Close 100% of open items across the TS/JS ecosystem plan, code review findings, docs, tests, and release.

**Current state:** MVP complete. TS/JS ecosystem ~85% done. 28/40 code review findings already fixed. No v0.2.0 tag cut.

---

## Phase 1: Remaining Code Review Findings (7 items)

These are the ONLY items still open from `CODE_REVIEW_REPORT.md`. Everything else is already fixed.

### 1.1 — L8: Add `stale_baseline: warn` test
- **What:** Add integration test asserting that `stale_baseline: warn` (the default) does NOT block the gate when stale entries exist.
- **File:** `tests/integration.rs` or `tests/mvp_gate_baseline.rs`
- **AC:** Test creates a baseline with stale entries, runs check with `stale_baseline: warn`, asserts exit code 0.

### 1.2 — L9: Add perf budget exceeded failure path test
- **What:** Add a test that verifies the perf budget gate actually fails when the budget is exceeded (e.g., set budget to 1ms).
- **File:** `tests/perf_budget.rs`
- **AC:** Test sets an impossibly low budget, runs check, asserts the budget-exceeded path triggers correctly.

### 1.3 — N2: Eliminate `AnonymizedTelemetrySummary` structural duplication
- **What:** Derive `AnonymizedTelemetrySummary` from `VerdictSummary` using a subset pattern or macro, rather than duplicating 6 fields.
- **File:** `src/verdict/mod.rs`
- **AC:** No manually duplicated field definitions. `From<VerdictSummary>` impl still works. All existing tests pass.

### 1.4 — N6: Share perf budget defaults between script and YAML
- **What:** Have `perf-tier1.yml` pass defaults explicitly via env vars sourced from a single place, or have the YAML not duplicate the defaults and rely solely on the script.
- **Files:** `.github/workflows/perf-tier1.yml`, `scripts/ci/perf_budget.sh`
- **AC:** Defaults exist in exactly one place. No drift risk.

### 1.5 — N8: Eliminate remaining doc overlap on Tag/Automation
- **What:** Remove the overlapping Tag/Automation mapping from `DOGFOOD_RELEASE_CHANNEL.md` and point to `support-matrix-v1.md` as the single source.
- **Files:** `docs/DOGFOOD_RELEASE_CHANNEL.md`, `docs/support-matrix-v1.md`
- **AC:** Tag/automation mapping lives in one doc only. Cross-reference in the other.

### 1.6 — L2: Clarify `--refresh` help text re: metadata reset
- **What:** Update `--refresh` clap help text to mention that `generated_from` metadata is reset to current tool version.
- **File:** `src/cli/mod.rs` (the clap doc comment on the refresh flag)
- **AC:** Help text mentions metadata refresh.

### 1.7 — M12: Fix fragile telemetry test with shared TempDir
- **What:** Isolate the 4 telemetry sub-cases in `wave2c_cli_integration.rs` so they each use their own TempDir/config rather than mutating a shared one.
- **File:** `tests/wave2c_cli_integration.rs`
- **AC:** Each sub-case is independent. Test passes when run in any order.

---

## Phase 2: TS/JS Ecosystem Plan Completion (4 items)

### 2.1 — P2.2: Complete doctor/snapshot schema alignment
- **What:** Ensure structured snapshot generated by npm wrapper is accepted by `doctor compare` with correct mismatch category tags (`extension_alias`, `condition_names`, `paths`, `exports`). Add golden test locking schema version.
- **Files:** `src/cli/doctor.rs`, `npm/specgate/src/generate-resolution-snapshot.js`, new test fixture
- **AC:** Round-trip test: npm snapshot → `doctor compare` → structured categories in output. Golden test locks schema version.

### 2.2 — P3.2: npm wrapper hardening (remaining items)
- **What:** Most H/M issues already fixed. Remaining: fresh-environment smoke test for `npx specgate check` on a TS fixture, and verify binary fetch/selection across OS/arch matrix in release workflow.
- **Files:** `npm/specgate/`, CI workflow
- **AC:** `npm test` includes a smoke test for the happy path. Release workflow includes OS/arch matrix verification step.

### 2.3 — P3.3: OpenClaw-scale regression gate in CI
- **What:** Fixture exists at `tests/fixtures/openclaw-scale/`. Wire it into CI so PRs touching `src/parser/`, `src/resolver/`, or `src/rules/` trigger the regression suite. Enforce perf budget.
- **Files:** `.github/workflows/mvp-merge-gate.yml` or new workflow, `tests/tsjs_openclaw_regression.rs`
- **AC:** CI runs OpenClaw-scale regression on relevant PRs. Budget threshold enforced.

### 2.4 — P3.1: Nearest-tsconfig multi-context resolver
- **What:** Per-file tsconfig context selection for repos with nested tsconfigs/project refs. Root-tsconfig fast path preserved.
- **Files:** `src/resolver/mod.rs`, new resolver context cache, integration fixtures
- **AC:** Nested-tsconfig fixture resolves using owning tsconfig. Single-root path benchmark-neutral (±5%). Resolver cache hit rate above threshold.
- **Note:** This was explicitly deferred from MVP. Most complex item remaining. Consider whether to include in v0.2.0 or defer to v0.3.0.

---

## Phase 3: Missing Test Coverage (from code review gaps table)

### 3.1 — `baseline --refresh` metadata provenance test
- **What:** Test that `generated_from` in refreshed baseline reflects current tool version/git SHA, not the stale original.
- **File:** `tests/integration.rs` or `src/baseline/mod.rs` unit tests
- **AC:** Test creates baseline with old metadata, refreshes, asserts metadata updated.
- **Note:** The audit found this test already exists at `baseline/mod.rs:858` (`refresh_baseline_uses_fresh_metadata`). **Verify this is sufficient — may already be DONE.**

### 3.2 — `handle_check_with_diff` + stale policy interaction test
- **What:** Test that diff mode + stale baseline policy interact correctly.
- **File:** `tests/wave2c_cli_integration.rs` or `tests/integration.rs`
- **AC:** Test with stale entries in diff mode, assert correct governance output.

### 3.3 — Telemetry schema completeness test
- **What:** Assert `schema_version`, `project_fingerprint`, and `summary` fields in telemetry events, not just `event`.
- **File:** `tests/wave2c_cli_integration.rs`
- **AC:** All telemetry schema fields validated.

### 3.4 — Structured snapshot multi-edge round-trip test
- **What:** Round-trip test with multi-edge fixture (not just single-edge).
- **File:** Relevant doctor/snapshot test file
- **AC:** Fixture with multiple resolution edges round-trips correctly.

### 3.5 — `--structured-snapshot-out` with absolute path test
- **What:** Test that `--structured-snapshot-out` works with both relative and absolute paths.
- **File:** Relevant CLI integration test
- **AC:** Absolute path test added alongside existing relative path test.

---

## Phase 4: Update CODE_REVIEW_REPORT.md

### 4.1 — Mark fixed items in the report
- **What:** Add resolution status to each finding in `CODE_REVIEW_REPORT.md`. Mark the 28+ fixed items as resolved with commit refs. Mark remaining items with their fix status from this plan.
- **File:** `CODE_REVIEW_REPORT.md`
- **AC:** Every finding has a resolution status. Report reflects ground truth.

---

## Phase 5: Release v0.2.0

### 5.1 — Update CHANGELOG [Unreleased] section
- **What:** Move any post-0.2.0-changelog work into [Unreleased] or add to [0.2.0] section as appropriate. Add TS/JS ecosystem entries for work done since the original 0.2.0 entry.
- **File:** `CHANGELOG.md`
- **AC:** CHANGELOG accurately reflects all work in 0.2.0.

### 5.2 — Final CI gate run
- **What:** Run `./scripts/ci/mvp_gate.sh` and ensure clean pass.
- **AC:** Exit code 0. All tests pass. No warnings.

### 5.3 — Tag and release v0.2.0
- **What:** `git tag v0.2.0 && git push origin v0.2.0` to trigger release workflows.
- **AC:** Tag exists. Release binaries built. npm package published (if ready).

---

## Execution Order

```
Phase 1 (code review fixes) — all items are independent, can be parallelized
  ↓
Phase 2.1–2.3 (ecosystem completion) — can parallel with Phase 3
Phase 2.4 (P3.1 nearest-tsconfig) — DECISION NEEDED: include in v0.2.0 or defer?
  ↓
Phase 3 (test coverage gaps) — can parallel with Phase 2
  ↓
Phase 4 (update report) — after Phase 1–3 complete
  ↓
Phase 5 (release) — after everything else
```

## Decision Required

**P3.1 (nearest-tsconfig):** This is high complexity and was explicitly deferred from MVP. Options:
- **A) Include in v0.2.0** — delays release, adds the most complex remaining item
- **B) Defer to v0.3.0** — ship v0.2.0 with everything else done, track P3.1 as first item for next release

All other items are straightforward and can be completed without architectural decisions.

---

## Item Count

| Phase | Items | Estimated Complexity |
|-------|-------|---------------------|
| Phase 1: Code review fixes | 7 | Low–Medium each |
| Phase 2: Ecosystem completion | 3–4 | Medium–High |
| Phase 3: Test coverage | 4–5 | Low each |
| Phase 4: Report update | 1 | Low |
| Phase 5: Release | 3 | Low |
| **Total** | **18–20** | |
