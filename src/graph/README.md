# graph/

Deferred to later phases.

Phase 1 intentionally stops at file-edge extraction and resolution primitives.
Dependency graph construction is built on top of parser/resolver outputs in subsequent phases.
